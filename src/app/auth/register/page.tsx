import Register from '@/components/Pages/AuthPage/Register';
import React from 'react';

const page = () => {
    return (
        <div>
            <Register/>
        </div>
    );
};

export default page;